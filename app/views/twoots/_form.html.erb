<%= form_for(@twoot) do |f| %>
  <% if @twoot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@twoot.errors.count, "error") %> prohibited this twoot from being saved:</h2>

      <ul>
      <% @twoot.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_field :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
